@{
    Layout = null;
    string email = Request.Form["lastName"].Trim();
        string typvstr = Request.Form["typvsr"].Trim();
        //string city = Request.Form["city"].Trim();
        string mit = Request.Form["mit"].Trim();
        string lastname = Request.Form["lastName"].Trim();
        string firstname = Request.Form["firstName"].Trim();
        string linkvktg = Request.Form["link"].Trim();
    string typvstr=Request.Form["typvs"].Trim();  
        
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    <div>
    
    </div>
</body>
</html>
