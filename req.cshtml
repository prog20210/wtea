@{
    Layout = null;
    string email = Request.Form["lastName"].Trim();
        string trademark = Request.Form["trademark"].Trim();
        //string city = Request.Form["city"].Trim();
        string dep = Request.Form["dep"].Trim();
        string lastname = Request.Form["lastName"].Trim();
        string firstname = Request.Form["firstName"].Trim();
        string otch = Request.Form["otch"].Trim();
        string dolz = Request.Form["dolz"].Trim();
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    <div>
    
    </div>
</body>
</html>
